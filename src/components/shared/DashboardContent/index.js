import React from 'react'
import { Box, Button } from '@chakra-ui/react'
const DashboardContent = ({children}) => {
    return ( <Box marginLeft="350px" py={5}>{children}</Box> )
}

export default DashboardContent
